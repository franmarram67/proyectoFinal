{% extends 'base.html.twig' %}

{% block title %}Tournament Master{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="/css/ajax.css">
{% endblock %}

{% block body %}

<div class="slider common">
    {# <img src="/img/slider1.jpg"/> #}
</div>

<div class="body">

<main>
    <div class="mainHeader">
        <div class="mainHeaderTitle"><h1>Edit Tournament</h1></div>
    </div>

    <div class="create">
        <div class="form">
            {{ form_start(form) }}
                {{ form_row(form.title) }}
                {{ form_row(form.description) }}

                {# {{ form_label(form.startDate) }}
                {{ form_widget(form.startDate) }}
                {{ form_errors(form.startDate) }} #}
                {{ form_row(form.startDate) }}

                {# {{ form_row(form.videogame) }} #}
                <div class="createButton"><button>{{ button_label|default('Edit') }}</button></div>
                <div class="createButton">
                    <input type="hidden" id="tournamentId" value="{{tournament.id}}">
                    <a class="deletetournament">Delete</a>
                </div>
            {{ form_end(form) }}
        </div>
        {#  #}
    </div>

</main>

{# <h3>{{tournament.title}} - by {{tournament.creatorUser.username}}</h3>
<p>{{ tournament.description }}</p>
<p>VideoGame: {{ tournament.videogame }}</p>
<p>Places: {{tournament.players|length}}/20</p>
<h4>{{tournament.startDate|date('Y-m-d  H:i:s') }}</h4> #}



</div>

{% endblock %}

{% block javascripts %}
    <script src="/js/ajax.js"></script>
{% endblock %}