{% extends 'base.html.twig' %}

{% block title %}Edit Tournament{% endblock %}

{% block body %}

<div class="slider common">
    {# <img src="/img/slider1.jpg"/> #}
</div>

<div class="body">

<main>
    <div class="mainHeader">
        <div class="mainHeaderTitle"><h1>Edit Tournament</h1></div>
    </div>
    <div class="mainAdminContent">
        {{ include('tournament/_form.html.twig', {'button_label': 'Edit'}) }}

        {# <a href="{{ path('tournament_index') }}">back to list</a> #}

        {{ include('tournament/_delete_form.html.twig') }}
    </div>

</main>

    

</div>

{% endblock %}
