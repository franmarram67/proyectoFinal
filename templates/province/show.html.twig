{% extends 'base.html.twig' %}

{% block title %}Province{% endblock %}

{% block body %}

<div class="slider common">
    {# <img src="/img/slider1.jpg"/> #}
</div>

<div class="body">

<main>
    <div class="mainHeader">
        <div class="mainHeaderTitle"><h1>Province</h1></div>
    </div>
    <div class="mainAdminContent">
        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ province.id }}</td>
                </tr>
                <tr>
                    <th>Name</th>
                    <td>{{ province.name }}</td>
                </tr>
                <tr>
                    <th>Description</th>
                    <td>{{ province.description }}</td>
                </tr>
                <tr>
                    <th>Image</th>
                    <td><img src="/img/{{ province.image }}" width="100px" /></td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('province_index') }}">back to list</a>

        <a href="{{ path('province_edit', {'id': province.id}) }}">edit</a>

        {{ include('province/_delete_form.html.twig') }}
    </div>

</main>
    

</div>
{% endblock %}
